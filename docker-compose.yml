version: "3.8"

services:
  users:
    build:
      context: ./users
    ports:
      - "8000:8000"
    environment:
      PORT: 8000

  pictures:
    build:
      context: ./pictures
    ports:
      - "8001:8001"
    environment:
      PORT: 8001
