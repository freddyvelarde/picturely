version: "3.8"

services:
  users:
    build:
      context: ./users
    ports:
      - "8000:8000"
    environment:
      PORT: 8000

  media:
    build:
      context: ./media
    ports:
      - "8001:8001"
    environment:
      PORT: 8001
